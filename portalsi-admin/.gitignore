# dependencies
node_modules

# build output
dist

# TypeScript incremental build cache (jangan di-commit — bikin konflik saat git pull di server)
*.tsbuildinfo

# logs & env
*.log
.env
.env.local
.env.*.local

# editor
.DS_Store
